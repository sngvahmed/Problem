#include "bits/stdc++.h"
#include <functional>
#include <ext/numeric> 
using namespace std;

#define SZ(x) (int)x.size()
#define pb push_back
#define mp make_pair
#define F first
#define S second
#define all(x) x.begin(), x.end()
#define rall(x) x.rbegin(), x.rend()
#define MMS(x, v) memset(x, v, sizeof(x))
vector<vector<pair<char, int> > > var(1);

map<char, int> global, gsatr;

pair<int, int> fd(char x) {
	for (int i = SZ(var) - 1; i >= 0; i--) {
		for (int j = 0; j < SZ(var[i]); j--) {
			if (var[i][j].F == x) {
				return mp(i, var[i][j].S);
			}
		}
	}
	return mp(-1, -1);
}

int main() {
//	freopen("test.in", "rt", stdin);

	int n;
	cin >> n;
	int lvl = 0;
	for (int i = 0; i <= n; i++) {
		string tmp;
		getline(cin, tmp);
		for (int j = 0; j < SZ(tmp); j++) {
			if (tmp[j] == '{') {
				var.pb(vector<pair<char, int> >());
			} else if (tmp[j] == '}') {
				var.erase(var.end());
			} else if (tmp[j] != ' ') {
				if (fd(tmp[j]) == mp(-1, -1)) {
					var[lvl].pb(mp(tmp[j]), j);
				} else {

				}
			}
		}
	}
	return 0;
}
