#include "bits/stdc++.h"
#include <functional>
#include <ext/numeric> 
using namespace std;

#define SZ(x) (int)x.size()
#define pb push_back
#define mp make_pair
#define F first
#define S second
#define all(x) x.begin(), x.end()
#define rall(x) x.rbegin(), x.rend()
#define MMS(x, v) memset(x, v, sizeof(x))
#define SF(x) scanf("%d" , &x)
int lower, upper;
int precision , popCount;

// return fitness (return equation)
int fitness() {

}

int main() {
	srand(time(0));

	freopen("test.in", "rt", stdin);
	SF(lower), SF(upper), SF(precision);
	return 0;
}
